Index: codereview/models.py
===================================================================
--- codereview/models.py	(revision 373)
+++ codereview/models.py	(working copy)
@@ -494,7 +494,8 @@
     if '@' in nickname:
       nickname = nickname.split('@', 1)[0]
     assert nickname
-    return cls.get_or_insert(key, user=user, email=email, nickname=nickname,
+    return cls.get_or_insert(key, user=user,
+                             email=str(email), nickname=str(nickname),
                              fresh=True)
 
   @classmethod
